<% if (pages > 0) { %>
<ul class="pagination text-center">
  <% if (current == 1) { %>
  <li class="page-item disabled header-drop"><a class="page-link">First</a></li>
  <% } else { %>
  <li class="page-item">
    <a class="page-link header-drop" href="<%=home%>page=1">First</a>
  </li>
  <% } %> <% var i = (Number(current) > 5 ? Number(current) - 4 : 1) %> <% if (i
  !== 1) { %>
  <li class="page-item disabledp"><a class="page-link header-drop">...</a></li>
  <% } %> <% for (; i <= (Number(current) + 4) && i <= pages; i++) { %> <% if (i
  == current) { %>
  <li class="page-item active header-drop"><a class="page-link header-drop"><%= i %></a></li>
  <% } else { %>
  <li class="page-item">
    <a class="page-link header-drop" href="<%=home%>page=<%= i %>"><%= i %></a>
  </li>
  <% } %> <% if (i == Number(current) + 4 && i < pages) { %>
  <li class="page-item disabled"><a class="page-link header-drop">...</a></li>
  <% } %> <% } %> <% if (current == pages) { %>
  <li class="page-item disabled"><a class="page-link header-drop">Last</a></li>
  <% } else { %>
  <li class="page-item">
    <a class="page-link header-drop" href="<%=home%>page=<%= pages %>">Last</a>
  </li>
  <% } %>
</ul>
<% } %>
